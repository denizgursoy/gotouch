- name: Empty Project Layout
  reference: https://go.dev/
  url: https://github.com/denizgursoy/go-touch-projects/raw/main/compressed/empty.zip
  language: go
  questions:
    - direction: Do you want Dockerfile?
      canSkip: true
      choices:
        - choice: Yes
          dependencies:
            # dependency with latest
            - github.com/labstack/echo/v4
            # dependency with version
            - go.uber.org/zap@v1.23.0
          files:
            # file from internet
            - url: https://raw.githubusercontent.com/denizgursoy/go-touch-projects/main/Dockerfile
              pathFromRoot: Dockerfile
            # file from content
            - content: |
                lorem ipsum
                lorem ipsum
                lorem ipsum
                lorem ipsum
                lorem ipsum
              pathFromRoot: Dockerfile
          # values for templating
          # can be any type array, object, number, boolean and string
          values:
            #boolean
            isDocker: true
            # string
            myString: test
            # object
            myObject:
              x: value x
              y: value y
            # array
            myArray:
              - arrayValue1
              - arrayValue2
